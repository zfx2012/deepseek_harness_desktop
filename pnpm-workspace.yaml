# pnpm 11 build-script policy. Electron binaries are restored manually
# (see README, Development section), so build scripts stay disabled.
allowBuilds:
  electron: false
  electron-winstaller: false
