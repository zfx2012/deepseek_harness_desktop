diff --git a/templates/nsis/portable.nsi b/templates/nsis/portable.nsi
index e5436bb0f810058b52e15ce65b3d256f86dabc6f..a89b908fd24bba1a135ddfc805c509d9da183783 100644
--- a/templates/nsis/portable.nsi
+++ b/templates/nsis/portable.nsi
@@ -7,11 +7,12 @@ WindowIcon Off
 AutoCloseWindow True
 RequestExecutionLevel ${REQUEST_EXECUTION_LEVEL}
 
+; dsh-desktop patch: do NOT go silent while extracting. Without SPLASH_IMAGE
+; the default silent mode left users staring at a frozen exe for minutes
+; (38k bundled kernel files). Showing the standard NSIS instfiles window with
+; its progress bar makes the extraction visible; AutoCloseWindow still closes
+; it automatically before the app starts.
 Function .onInit
-  !ifndef SPLASH_IMAGE
-    SetSilent silent
-  !endif
-
   !insertmacro check64BitAndSetRegView
 FunctionEnd
 
